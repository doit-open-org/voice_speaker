# 小程序与设备 BLE 通信协议汇总

简体中文 | [English](./device-communication-protocol-en.md)

本文根据当前小程序源码整理，描述小程序通过 BLE 发送给设备的数据，以及小程序当前能够解析的设备回包。本文是“代码现状说明”，不是独立的固件协议原稿；源码无法确定的字段会明确标注为“待固件确认”。

整理日期：2026-08-20

## 1. 协议分层

小程序发送的数据分为两层：

```text
业务命令（CMD + LEN + DATA）
        ↓
BLE 传输帧（VER + SEQ + LEN + 业务命令分片 + CRC8）
        ↓
GATT Write Characteristic
```

设备通知数据走相反方向：小程序先校验并移除 BLE 传输帧，再把重组后的业务命令交给当前页面处理。

除非字段章节另有说明，本文中的多字节整数均按大端序（高字节在前）发送。

## 2. BLE GATT 参数

| 用途 | UUID |
| --- | --- |
| Service | `00001910-0000-1000-8000-00805F9B34FB` |
| 小程序写入设备 | `00002B12-0000-1000-8000-00805F9B34FB` |
| 设备通知小程序 | `00002B10-0000-1000-8000-00805F9B34FB` |

连接成功并开启 Notify 后，小程序立即执行 `CMD 0x00` 握手。当前连接流程没有调用 `wx.setBLEMTU`；小程序从 `CMD 0x00` 回包的最后 2 字节读取设备 MTU，再减去 3 字节 ATT 头，得到后续代码使用的可写长度 `M`。

```text
M = 设备返回的 MTU - 3
```

在收到设备 MTU 之前，`M` 的默认值是 `509`（即 `512 - 3`）。

## 3. BLE 传输帧

### 3.1 当前使用的 `0x04` 帧

每个业务命令在写入 GATT 前，会被切成一个或多个以下格式的传输帧：

| 偏移 | 长度 | 字段 | 说明 |
| ---: | ---: | --- | --- |
| 0 | 1 | `VER` | 固定为 `0x04` |
| 1 | 2 | `SEQ` | 大端序；bit15 为最后一帧标志，bit0~14 为从 0 开始的帧序号 |
| 3 | 2 | `DATA_LEN` | 本帧 `DATA` 的字节数，大端序 |
| 5 | N | `DATA` | 业务命令的一段数据 |
| 5+N | 1 | `CRC8` | 从 `VER` 到 `DATA` 最后一个字节的 CRC8 |

单帧业务数据最大长度：

```text
MAX_DATA_LEN = M - 6
```

其中 6 字节包括 `VER(1) + SEQ(2) + DATA_LEN(2) + CRC8(1)`。

最后一帧通过 `SEQ |= 0x8000` 标记。例如第一帧同时也是最后一帧时，`SEQ` 为 `0x8000`，线上字节为 `80 00`。

例如查询曲目列表的业务包是 `0D 00 00`，封装后的完整单帧数据是：

```text
04 80 00 00 03 0D 00 00 6D
|  |---| |---| |------|  |
VER SEQ   LEN    DATA   CRC8
```

发送队列每隔 30 ms 写入一个传输帧。当前实现没有在传输帧层增加确认、超时重传或滑动窗口。

### 3.2 CRC8

CRC8 参数如下：

| 参数 | 值 |
| --- | --- |
| 多项式 | `0x07` |
| 初始值 | `0x00` |
| 输入反射 | 否 |
| 输出异或 | `0x00` |
| 覆盖范围 | 当前传输帧除末尾 CRC8 外的全部字节 |

该参数组合等同于常见的 CRC-8/SMBUS 参数。源码使用 256 项查表实现。

### 3.3 接收重组行为

设备回包也必须使用上述 `0x04` 传输帧。当前小程序的处理顺序是：

1. 校验每个传输帧的 CRC8，失败则丢弃该帧。
2. 取偏移 5 到 CRC8 前的所有字节，追加到全局接收缓存。
3. 当 `SEQ` 高字节的 bit7 为 1 时，将缓存作为一个完整业务包交给页面。
4. 清空接收缓存，等待下一个业务包。

当前代码没有校验 `VER`、`DATA_LEN` 和帧序号连续性。

## 4. 业务命令通用格式

当前在用业务命令通常使用以下头部：

| 偏移 | 长度 | 字段 | 说明 |
| ---: | ---: | --- | --- |
| 0 | 1 | `CMD` | 命令号 |
| 1 | 2 | `LEN` | `DATA` 长度，大端序 |
| 3 | N | `DATA` | 命令数据 |

常见的无参数查询命令因此为 3 字节，例如查询曲目列表为 `0D 00 00`，查询容量为 `11 00 00`。

命令总览：

| CMD | 方向 | 用途 | 当前状态 |
| --- | --- | --- | --- |
| `0x00` | 小程序 -> 设备 | 连接握手、同步时间并查询设备信息/MTU | 在用 |
| `0x01` | 小程序 -> 设备 | 配网/绑定初始化（源码命名） | 在用 |
| `0x02` | 小程序 -> 设备 | 通用属性查询 | 已实现，当前页面未调用 |
| `0x03` | 小程序 -> 设备 | 通用属性设置 | 已实现，当前页面未调用 |
| `0x04` | 小程序 -> 设备 | 解绑 | 在用 |
| `0x0A` | 小程序 -> 设备 | 声明待传输文件 | 在用 |
| `0x0B` | 双向 | 设备请求文件块；小程序发送文件数据 | 在用 |
| `0x0C` | 设备 -> 小程序 | 文件传输最终结果 | 在用 |
| `0x0D` | 双向 | 查询/返回设备曲目列表 | 在用 |
| `0x0E` | 双向 | 删除曲目及结果 | 在用 |
| `0x0F` | 双向 | 播放曲目及结果 | 在用 |
| `0x10` | 双向 | 重命名曲目及结果 | 在用 |
| `0x11` | 双向 | 查询/返回设备容量 | 在用 |

## 5. 连接与设备管理命令

### 5.1 `CMD 0x00`：握手和 MTU 查询

小程序请求：

| 偏移 | 长度 | 字段 | 说明 |
| ---: | ---: | --- | --- |
| 0 | 1 | `CMD` | `0x00` |
| 1 | 2 | `LEN` | `0x0004` |
| 3 | 4 | `TIMESTAMP` | 当前 Unix 秒级时间戳，例外使用小端序 |

```text
00 00 04 <timestamp_le_u32>
```

例如时间戳数值为 `0x12345678` 时，业务包为：

```text
00 00 04 78 56 34 12
```

设备回包的完整字段定义在小程序中不完整。当前代码只使用回包最后 2 字节作为大端序 ATT MTU：

```text
device_mtu = (倒数第 2 字节 << 8) | 最后 1 字节
M = device_mtu - 3
```

收到 `CMD 0x00` 后，小程序立即发送 `CMD 0x01`。

### 5.2 `CMD 0x01`：配网/绑定初始化

当前实际发送的业务包固定为 156 字节：

| 业务包偏移 | 长度 | 内容 |
| ---: | ---: | --- |
| 0 | 1 | `CMD = 0x01` |
| 1 | 2 | `LEN = 0x0099`（153） |
| 3 | 102 | 全 `0x00` |
| 105 | 10 | 固定 Device Key 的 ASCII 字节 |
| 115 | 41 | 全 `0x00` |

当前 Device Key 固定为字符串：

```text
8910199948
```

对应字节：

```text
38 39 31 30 31 39 39 39 34 38
```

102 字节和 41 字节保留区的内部字段含义无法从小程序代码确定，需由固件协议确认。小程序收到任意 `CMD 0x01` 回包后即把设备标记为已连接，没有检查状态字段。

### 5.3 `CMD 0x04`：解绑

请求固定为：

```text
04 00 01 01
```

| 偏移 | 长度 | 字段 | 值 |
| ---: | ---: | --- | --- |
| 0 | 1 | `CMD` | `0x04` |
| 1 | 2 | `LEN` | `0x0001` |
| 3 | 1 | `ACTION` | `0x01` |

发送后，小程序立即清除本地设备缓存和连接展示，不等待 `CMD 0x04` 回包。

## 6. 文件传输协议

### 6.1 文件名编码

`CMD 0x0A` 和 `CMD 0x10` 中的文件名使用以下编码：

1. 前缀固定为 ASCII `\U`，即 `5C 55`。
2. JavaScript 字符串的每个 UTF-16 code unit 按小端序写 2 字节。

例如 `A.mp3` 编码为：

```text
5C 55 41 00 2E 00 6D 00 70 00 33 00
```

这不是 UTF-8。包含代理对的字符会按两个 UTF-16 code unit 分别编码。

### 6.2 文件 CRC16

文件总 CRC 和文件块 CRC 使用同一种 CRC16：

| 参数 | 值 |
| --- | --- |
| 多项式（反射形式） | `0xA001` |
| 初始值 | `0x0000` |
| 输入/输出 | 逐字节低位优先的查表算法 |
| 输出异或 | `0x0000` |
| 在线字节序 | 计算结果作为大端序 `uint16` 写入 |

注意：该算法初始值是 `0x0000`，不是 Modbus 常用的 `0xFFFF`。

### 6.3 `CMD 0x0A`：声明文件

| 偏移 | 长度 | 字段 | 说明 |
| ---: | ---: | --- | --- |
| 0 | 1 | `CMD` | `0x0A` |
| 1 | 2 | `LEN` | `6 + FILE_NAME_LEN` |
| 3 | 4 | `FILE_SIZE` | 完整音频字节数，大端序 |
| 7 | 2 | `FILE_CRC` | 完整音频的 CRC16，大端序 |
| 9 | N | `FILE_NAME` | 上述 `\U + UTF-16LE` 编码 |

按照当前页面的读取方式，设备成功回包预计为：

```text
0A 00 01 <status>
```

小程序只定义了 `status = 0` 为成功，所有非 0 值均按“发送文件信息失败”处理。

### 6.4 `CMD 0x0B`：设备请求文件块

设备通过 Notify 请求下一块文件：

| 偏移 | 长度 | 字段 | 说明 |
| ---: | ---: | --- | --- |
| 0 | 1 | `CMD` | `0x0B` |
| 1 | 2 | `LEN` | 预计为 `0x0008`，当前代码不校验 |
| 3 | 4 | `OFFSET` | 从文件开头起的字节偏移，大端序 |
| 7 | 4 | `CHUNK_SIZE` | 本次期望块大小，大端序 |

小程序会实际发送：

```text
actual_chunk_size = min(CHUNK_SIZE, FILE_SIZE - OFFSET)
```

### 6.5 `CMD 0x0B`：小程序发送文件数据

设备请求的一块数据还会被拆成若干个业务包。每次响应新的块请求时，低 14 位序号重新从 0 开始。

| 偏移 | 长度 | 字段 | 说明 |
| ---: | ---: | --- | --- |
| 0 | 1 | `CMD` | `0x0B` |
| 1 | 2 | `LEN` | `2 + 可选 BLOCK_CRC(2) + DATA_LEN` |
| 3 | 2 | `FILE_SEQ` | 大端序，包含序号与结束标志 |
| 5 | 2 | `BLOCK_CRC` | 仅低 14 位序号为 0 的首包携带 |
| 5 或 7 | N | `DATA` | 音频原始字节 |

`FILE_SEQ` 位定义：

| 位 | 名称 | 说明 |
| ---: | --- | --- |
| 15 | `FILE_END` | 当前包同时是整个文件最后一包时置 1 |
| 14 | `BLOCK_END` | 当前包是本次设备请求块的最后一包时置 1 |
| 13..0 | `SEQ` | 当前请求块内的包序号，从 0 开始 |

首包的 `BLOCK_CRC` 是对本次设备请求的完整数据块计算的 CRC16，而不是只对首包数据计算。

为了给外层 `0x04` 帧留出 6 字节，小程序将每个 `CMD 0x0B` 业务包限制在 `M - 6` 字节以内。因此：

```text
首个业务包的最大音频数据 = M - 13
后续业务包的最大音频数据 = M - 11
```

当 `M = 509` 时，首包最多携带 496 字节音频，后续包最多携带 498 字节音频。

### 6.6 `CMD 0x0C`：文件传输结果

按照当前页面的读取方式，设备在整个文件处理结束后预计回复：

```text
0C 00 01 <status>
```

小程序只区分：

| `status` | 处理 |
| --- | --- |
| `0x00` | 传输成功 |
| 非 `0x00` | 传输失败 |

源码注释提到了以下失败原因，但没有给出数值映射：App 主动取消、设备主动取消、写失败、数据越界、文件总 CRC 错误、块 CRC 错误、序号错误。具体状态码需由固件协议补充。

### 6.7 文件传输时序

```text
小程序                                  设备
  |                                      |
  |-- CMD 0x0A 文件大小/CRC/名称 ------->|
  |<------------- CMD 0x0A status -------|
  |<-- CMD 0x0B offset + chunk_size -----|
  |-- CMD 0x0B seq=0 + 块CRC + 数据 ---->|
  |-- CMD 0x0B seq=1... + 数据 ---------->|
  |-- CMD 0x0B BLOCK_END + 数据 -------->|
  |<-- CMD 0x0B 下一 offset/chunk_size --|
  |                  ...                  |
  |-- CMD 0x0B FILE_END|BLOCK_END ------>|
  |<------------- CMD 0x0C status -------|
```

设备控制块偏移和大小，小程序不主动按固定 10 KiB 连续推送。源码注释示例中的常见 `CHUNK_SIZE` 为 10240，但代码接受设备给出的任意 32 位值。

## 7. 设备曲目管理命令

### 7.1 `CMD 0x0D`：查询曲目列表

请求：

```text
0D 00 00
```

设备响应业务包头为：

```text
0D <len_be_u16> <一个或多个曲目记录>
```

每条曲目记录按当前解析代码推导如下：

| 记录内偏移 | 长度 | 字段 | 说明 |
| ---: | ---: | --- | --- |
| 0 | 1 | `ITEM_LEN` | 当前记录总字节数，包含本字段 |
| 1 | 2 | `FILE_INDEX` | 大端序 |
| 3 | 4 | `FILE_SIZE` | 大端序 |
| 7 | 1 | `NAME_TYPE` | `0x01` 中文/中英文，`0x02` 纯英文 |
| 8 | N | `FILE_NAME` | 名称数据 |

当前页面对名称数据的实际解码行为是：

- `NAME_TYPE = 0x01`：每 2 字节作为一个 `uint16` 字符单元，再逆序拼接字符单元。
- 其他值（包括 `0x02`）：逐字节转字符，再逆序拼接全部字节。

名称响应的端序和是否包含 `\U` 前缀无法仅从小程序确定，固件实现应以真机联调结果确认。

### 7.2 `CMD 0x0E`：删除曲目

请求：

| 偏移 | 长度 | 字段 |
| ---: | ---: | --- |
| 0 | 1 | `CMD = 0x0E` |
| 1 | 2 | `LEN = 0x0002` |
| 3 | 2 | `FILE_INDEX`，大端序 |

例如删除索引 2：

```text
0E 00 02 00 02
```

设备响应预计为 `0E 00 01 <status>`；`0` 成功，非 `0` 失败。

### 7.3 `CMD 0x0F`：播放曲目

格式与删除命令相同，仅命令号改为 `0x0F`：

```text
0F 00 02 <file_index_be_u16>
```

设备响应预计为 `0F 00 01 <status>`；当前页面只在非 0 时提示播放失败。

### 7.4 `CMD 0x10`：重命名曲目

| 偏移 | 长度 | 字段 | 说明 |
| ---: | ---: | --- | --- |
| 0 | 1 | `CMD` | `0x10` |
| 1 | 2 | `LEN` | `2 + OLD_NAME_LEN + NEW_NAME_LEN` |
| 3 | 1 | `OLD_NAME_LEN` | 旧名称编码后的字节数 |
| 4 | 1 | `NEW_NAME_LEN` | 新名称编码后的字节数 |
| 5 | N | `OLD_NAME` | `\U + UTF-16LE` |
| 5+N | M | `NEW_NAME` | `\U + UTF-16LE` |

两个名称长度字段只有 1 字节。当前代码没有在发送前校验名称编码长度是否超过 255。

设备响应预计为 `10 00 01 <status>`；`0` 成功，非 `0` 失败。

### 7.5 `CMD 0x11`：查询容量

请求：

```text
11 00 00
```

设备响应按当前页面读取方式为：

| 偏移 | 长度 | 字段 | 说明 |
| ---: | ---: | --- | --- |
| 0 | 1 | `CMD = 0x11` | |
| 1 | 2 | `LEN` | 预计为 `0x0008`，当前代码不校验 |
| 3 | 4 | `TOTAL_CAPACITY` | 总容量，大端序 |
| 7 | 4 | `FREE_CAPACITY` | 剩余容量，大端序 |

页面使用率计算方式：

```text
used_percent = round((TOTAL_CAPACITY - FREE_CAPACITY) / TOTAL_CAPACITY * 100)
```

## 8. 通用属性命令（当前未使用）

`hextool.js` 还保留了 `CMD 0x02` 和 `CMD 0x03` 构造器，但仓库内没有页面调用它们。它们不遵循当前业务命令统一的 2 字节 `LEN` 头，接入前应与固件重新确认。

### 8.1 `CMD 0x02`：查询属性

```text
02 <attr_count_u8> <attr_id_1_u8> ... <attr_id_n_u8>
```

### 8.2 `CMD 0x03`：设置属性

每个属性独立拼接一段：

```text
03 <1 + value_len> <attr_id_u8> <value>
```

数值小于 256 时编码为 1 字节；更大的数值编码为 2 字节小端序。字符串值被当作不带分隔符的十六进制字符串转换为字节。

## 9. 旧版 20 字节传输帧（当前未启用）

源码保留了旧封包函数 `bleDatasFix`，但 `sendDatas` 已无条件调用新版 `0x04` 封包，以下格式不会在当前版本发出：

| 偏移 | 长度 | 字段 | 说明 |
| ---: | ---: | --- | --- |
| 0 | 1 | `VER` | `0x01` |
| 1 | 1 | `SEQ` | bit7 为最后一包，低 7 位为序号 |
| 2 | 17 | `DATA` | 不足 17 字节补 `0x00` |
| 19 | 1 | `CRC8` | 覆盖前 19 字节 |

旧版 `CMD 0x01` 还存在 1 字节长度的 153/163 字节变体，同样未被当前页面使用。由于旧分包代码在业务长度恰好为 17 的整数倍时不会设置最后一包标志，不应把该实现直接作为新固件兼容依据。

## 10. 当前实现的边界与待确认项

以下事项对固件联调有直接影响：

1. `CMD 0x00` 回包除末尾 MTU 外的字段定义，源码没有解析。
2. `CMD 0x01` 的两个全零保留区含义，以及回包状态格式，源码没有定义。
3. 文件传输失败状态只有语义名称，没有数值枚举。
4. 曲目列表名称的设备侧存储顺序、端序和 `\U` 前缀规则，需要用真实回包确认。
5. 小程序接收外层帧时不校验版本、声明长度或序号；固件仍应正确填写这些字段。
6. 小程序没有传输帧级重试。设备如发现 CRC8、块 CRC16 或序号错误，需要通过业务状态/下一次块请求驱动恢复。
7. 所有 BLE 写入共用一个模块级队列，固定 30 ms 间隔；文件数据发送期间不应并发插入其他设备命令。

## 11. 源码索引

| 内容 | 源码 |
| --- | --- |
| GATT UUID、连接、Notify、写入 | [`utils/bletool.js`](../utils/bletool.js) |
| 外层分包、CRC8、握手/绑定/解绑、回包重组 | [`utils/hextool.js`](../utils/hextool.js) |
| CRC16、文件名编码、文件和曲目命令 | [`utils/operationFile.js`](../utils/operationFile.js) |
| 连接握手和 MTU 处理时序 | [`pages/device/device.js`](../pages/device/device.js) |
| 生成音频发送流程 | [`pages/generate/generate.js`](../pages/generate/generate.js) |
| 本地 MP3 导入、曲目和容量回包处理 | [`pages/devMusicList/devMusicList.js`](../pages/devMusicList/devMusicList.js) |
